<script src="js/socket.io.js"></script>
<script>
const socket = io("http://localhost:3000"); // your backend URL

function submitData() {

    const packName = document.getElementById("stationName").value.trim();
    const packNo = document.getElementById("packNo").value.trim();
    let barcode = document.getElementById("barcode").value.trim();

    // Validate Pack Name
    if(packName === ""){
        alert("Please select Pack Name");
        return;
    }

    // Validate Pack No (must be 6 digits)
    const packNoRegex = /^\d{6}$/;
    if(!packNoRegex.test(packNo)){
        alert("Pack No must be exactly 6 digits");
        return;
    }

    // If barcode empty send NA
    if(barcode === ""){
        barcode = "na";
    }

    const data = {
        pack_name: packName,
        pack_no: packNo,
        smoke_sensor: barcode
    };

    // Emit data to backend via Socket.IO
    socket.emit("submitPackData", data);
}

// Listen for backend response
socket.on("packResponse", function(status, message){
    alert(message);

    if(status === "success"){
        // Clear form after successful submit
        document.getElementById("packNo").value = "";
        document.getElementById("barcode").value = "";
        document.getElementById("stationName").value = "";
        document.getElementById("stationName").focus();
    }
});
</script>
