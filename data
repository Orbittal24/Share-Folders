CREATE TABLE ocv_process_log (
    id INT IDENTITY(1,1) PRIMARY KEY,
    cellqr NVARCHAR(100),
    stage NVARCHAR(50),   -- INSERT_SQL / UPDATE_SQL / REDIS / DELETE_SOURCE / ERROR
    status NVARCHAR(20),  -- SUCCESS / FAIL
    message NVARCHAR(MAX),
    log_time DATETIME DEFAULT GETDATE()
)
