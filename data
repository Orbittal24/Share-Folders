SELECT TOP 1 Process_ID, Running_Count, Pack_ID, Angle_Array
        FROM taco_treceability.Process_Register_master
        WHERE Torque_Array = ?
          AND CHARINDEX(',' + ?, ',' + Angle_Array + ',') > 0
        ORDER BY Process_ID DES
