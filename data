try:
    # SQL
    dst_cursor.execute(...)
    dst_conn.commit()

    # Redis ONLY if SQL success
    r.setex(...)

    # DELETE ONLY if everything success
    src_cursor.execute(...)
    src_conn.commit()

except Exception as e:
    print("FAILED - NO DELETE, NO REDIS")
