First — add the path variable

At the top of your file, you already have:

var fs = require('fs');

Immediately after this line, add:

const MASTER_DATA_FILE = "C:\\Project\\Services\\OCV\\masterDataArray.json";

So it becomes:

var W3CWebSocket = require('websocket').w3cwebsocket;
var fs = require('fs');

const MASTER_DATA_FILE = "C:\\Project\\Services\\OCV\\masterDataArray.json";
Point 1 — Line 41

Currently you have:

fs.readFile("masterDataArray.json", function (err, data) {

Change it to:

fs.readFile(MASTER_DATA_FILE, function (err, data) {

This is the file reading location.

Point 2 — Line 80

Currently:

fs.writeFile("masterDataArray.json", JSON.stringify(objArr, null, 2), err => {

Change it to:

fs.writeFile(MASTER_DATA_FILE, JSON.stringify(objArr, null, 2), err => {

This is the write operation after the WebSocket sends/receives data.

Point 3 — Line 255

Currently:

fs.writeFile("masterDataArray.json", JSON.stringify(objArr, null, 2), err => {

Change it to:

fs.writeFile(MASTER_DATA_FILE, JSON.stringify(objArr, null, 2), err => {

This is the write operation when new OPC UA data is received.

Final result

You should have only these 4 relevant changes:

var fs = require('fs');

const MASTER_DATA_FILE = "C:\\Project\\Services\\OCV\\masterDataArray.json";

Then:

fs.readFile(MASTER_DATA_FILE, function (err, data) {

and both:

fs.writeFile(MASTER_DATA_FILE, JSON.stringify(objArr, null, 2), err => {

So you do not need to change any other logic or flow.

If you give me your new exact folder path, I can also show you the exact line to paste with your path.
