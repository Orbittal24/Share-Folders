
redis_client = redis.Redis(
    host='localhost',
    port=6379,
    db=0,
    password='Gotion@123Strong!',
    decode_responses=True
)


redis_host = "localhost"
redis_port = 6379
redis_db = 31
redis_password = "Gotion@123Strong!"

r = redis.StrictRedis(
    host=redis_host,
    port=redis_port,
    password=redis_password,   # 👈 add this line
    db=redis_db,
    decode_responses=True
)
