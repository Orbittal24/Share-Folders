else if (Number(globarr.status) === 1 && newcycle === 1) {

    console.log("DISPLAY BLOCK EXECUTED");

    document.getElementById('forceValue').innerText =
        Number(globarr.force).toFixed(3);

    document.getElementById('loadValue').innerText =
        Number(globarr.load).toFixed(3);
}
