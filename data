} else if (message === "Process already completed. No update necessary.") {
  await pool1.request().query(`
    DELETE FROM taco_treceability.torque_details_EIP_mirror
    WHERE pack_no='${row.pack_no}' AND module_barcode='${row.module_barcode}' 
      AND process_name='${row.process_name}' AND pack_name='${row.pack_name}'
  `);
  continue;
}
✅ With THIS:
js
Copy code
} else if (message === "Process already completed. No update necessary.") {
  await deleteRow(row.sr_no);
  continue;
}
