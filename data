<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Station Status</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<style>
body {
    background: #f4f6f9;
    font-family: Arial, sans-serif;
}

/* ===== CARD ===== */
.card {
    background: #fff;
    max-width: 1000px;
    margin: 40px auto;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 6px 18px rgba(0,0,0,.15);
}

h2 {
    color: #2e7d32;
    margin-bottom: 25px;
}

/* ===== FORM ROW ===== */
.row {
    display: flex;
    gap: 24px;
    align-items: flex-start;
    flex-wrap: wrap;
}

/* ===== FORM GROUPS ===== */
.group {
    display: flex;
    flex-direction: column;
}

/* Station wide */
.group.station {
    flex: 3;
    min-width: 250px;
}

/* Line small */
.group.line {
    flex: 1;
    max-width: 120px;
}

/* Barcode normal */
.group.barcode {
    flex: 2;
    min-width: 220px;
}

label {
    font-size: 14px;
    margin-bottom: 6px;
    color: #444;
    font-weight: bold;
}

/* ===== INPUTS ===== */
input, select {
    width: 100%;
    height: 45px;
    font-size: 16px;
    padding: 6px 10px;
    border-radius: 6px;
    border: 1px solid #ccc;
    box-sizing: border-box;
}

/* Barcode emphasis */
#barcode {
    font-size: 18px;
    letter-spacing: 1px;
}

/* ===== BUTTON ===== */
.button-row {
    display: flex;
    justify-content: center;
    margin-top: 30px;
}

button {
    height: 45px;
    width: 160px;
    font-size: 16px;
    font-weight: bold;
    background: #28a745;
    border: none;
    color: white;
    border-radius: 6px;
    cursor: pointer;
}

button:hover {
    background: #218838;
}

/* ===== POPUP ===== */
.popup-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,.4);
    justify-content: center;
    align-items: center;
}

.popup {
    background: #fff;
    padding: 25px 30px;
    border-radius: 8px;
    text-align: center;
    min-width: 300px;
}

.popup h3 {
    margin-bottom: 20px;
    color: #2e7d32;
}
</style>
</head>

<body>

<div class="card">
    <h2>Station Status OK</h2>

    <div class="row">

        <div class="group station">
            <label>Station Name</label>
            <input list="stations" id="stationName" placeholder="Search Station">
            <datalist id="stations"></datalist>
        </div>

        <div class="group line">
            <label>Line</label>
            <select id="line">
                <option value="1">1</option>
            </select>
        </div>

        <div class="group barcode">
            <label>Barcode</label>
            <input type="text" id="barcode" placeholder="Scan Barcode" autofocus>
        </div>

    </div>

    <div class="button-row">
        <button onclick="submitData()">Submit</button>
    </div>
</div>

<!-- ===== POPUP ===== -->
<div class="popup-overlay" id="popup">
    <div class="popup">
        <h3 id="popupMessage"></h3>
        <button onclick="closePopup()">OK</button>
    </div>
</div>

<script>
/* ========= STATION MAP ========= */
let stationMap = {};

/* ========= LOAD STATIONS ========= */
fetch('/api/stations')
    .then(res => res.json())
    .then(data => {
        const list = document.getElementById('stations');

        data.forEach(r => {
            stationMap[r.Station_Name] = r.Station_ID;

            const opt = document.createElement('option');
            opt.value = r.Station_Name;
            list.appendChild(opt);
        });
    });

/* ========= SUBMIT ========= */
function submitData() {
    const stationName = document.getElementById('stationName').value.trim();
    const stationId = stationMap[stationName];
    const line = document.getElementById('line').value;
    const barcode = document.getElementById('barcode').value.trim();

    if (!stationId || !barcode) {
        showPopup("Select valid station and scan barcode");
        return;
    }

    fetch('/api/scan', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ stationId, line, barcode })
    })
    .then(res => res.json())
    .then(resp => {
        const apiMsg =
            resp.externalApiResponse?.message ||
            resp.externalApiResponse?.msg ||
            resp.message;

        if (apiMsg === "Station created/updated successfully.") {
            showPopup("Station status updated");
        } else {
            showPopup(apiMsg || "Unknown response");
        }
    });
}

/* ========= POPUP ========= */
function showPopup(msg) {
    document.getElementById('popupMessage').innerText = msg;
    document.getElementById('popup').style.display = "flex";
}

function closePopup() {
    document.getElementById('popup').style.display = "none";
    location.reload(); // reset for next scan
}
</script>

</body>
</html>
