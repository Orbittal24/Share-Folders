const axios = require("axios");

if (status.includes("INTERLOCK")) {

    valueupdate = 0;

    Swal.fire({
        title: "Interlock Pending",
        text: "Previous station process is pending.",
        icon: "warning",
        confirmButtonText: "OK",
        allowOutsideClick: false
    }).then(() => {
        window.location.href = "/";
    });

}
else if (status.includes("NOT")) {

    valueupdate = 0;

    Swal.fire({
        title: `Cell scanning is Pending for module ${barcode}`,
        icon: "info",
        confirmButtonText: "OK",
        allowOutsideClick: false
    }).then(() => {
        window.location.href = "/";
    });

}
else {

    valueupdate = 1;

}
